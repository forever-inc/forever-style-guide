<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to forever_style_guide_path, class: "navbar-brand" do %>
        <%= image_tag("forever_style_guide/logo-160.png", alt: "Forever Logo") %>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-left">
        <% @sections.each_with_index do |section, index| %>
          <li class="style-guide-bar-section <%= (section == @current_section) ? 'active'.html_safe : '' %>">
            <%= link_to section.title, forever_style_guide_path + section.id %>
          </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <form class="form-inline">
          <div class="form-group">
            <label>
              <input id="style-guide-checkbox" type="checkbox" /> Pristine?
            </label>
          </div>
        </form>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
